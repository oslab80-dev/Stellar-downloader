<Window x:Class="StellarDownloader.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Stellar Build Repairer" Height="450" Width="600" 
        Background="#2b2d31" WindowStartupLocation="CenterScreen" ResizeMode="NoResize">
    <Grid Margin="25">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Label Grid.Row="0" Content="Stellar Auto-Downloader Tool" FontSize="22" FontWeight="Bold" Foreground="#f2f3f5" Padding="0,0,0,15"/>

        <StackPanel Grid.Row="1" Margin="0,0,0,20">
            <TextBlock Text="SELECT STELLAR / FORTNITE DIRECTORY:" FontSize="11" FontWeight="Bold" Foreground="#b5bac1" Margin="0,0,0,5"/>
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="110"/>
                </Grid.ColumnDefinitions>
                <TextBox x:Name="TxtFolder" Grid.Column="0" Height="30" VerticalContentAlignment="Center" Background="#383a40" Foreground="#dbdee1" BorderBrush="#4e5058" IsReadOnly="True" Padding="5,0"/>
                <Button x:Name="BtnBrowse" Grid.Column="1" Content="Browse..." Margin="10,0,0,0" Height="30" Background="#5865f2" Foreground="White" FontWeight="Bold" BorderThickness="0" Click="BtnBrowse_Click">
                    <Button.Resources>
                        <Style TargetType="Border">
                            <Setter Property="CornerRadius" Value="4"/>
                        </Style>
                    </Button.Resources>
                </Button>
            </Grid>
        </StackPanel>

        <StackPanel Grid.Row="2" Margin="0,0,0,20">
            <TextBlock Text="PASTE DOWNLOAD CODE FROM WEBSITE:" FontSize="11" FontWeight="Bold" Foreground="#b5bac1" Margin="0,0,0,5"/>
            <TextBox x:Name="TxtCode" Height="80" TextWrapping="Wrap" AcceptsReturn="True" VerticalScrollBarVisibility="Auto" Background="#383a40" Foreground="#23a55a" BorderBrush="#4e5058" FontFamily="Consolas" Padding="5"/>
        </StackPanel>

        <Button x:Name="BtnStart" Grid.Row="3" Content="START REPAIR &amp; DOWNLOAD" Height="40" Background="#23a55a" Foreground="White" FontSize="14" FontWeight="Bold" BorderThickness="0" Click="BtnStart_Click">
            <Button.Resources>
                <Style TargetType="Border">
                    <Setter Property="CornerRadius" Value="4"/>
                </Style>
            </Button.Resources>
        </Button>

        <TextBlock x:Name="LblStatus" Grid.Row="4" Text="Status: Ready." FontSize="13" Foreground="#949ba4" Margin="0,15,0,5"/>
        <ProgressBar x:Name="PrgBar" Grid.Row="5" Height="20" VerticalAlignment="Top" Background="#1e1f22" Foreground="#23a55a" BorderThickness="0"/>
    </Grid>
</Window>